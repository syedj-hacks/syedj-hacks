BANNER — dark.svg / light.svg

These two files are code-generated (terminal-mock, ~1200x480), not raster art —
regenerate by editing the SVG directly, there's no external asset to drop in here.

If you later switch to the dot-matrix portrait direction instead:
  required input: portrait.png (plain/solid background, head-and-shoulders,
  at least 800x800, well-lit, facing camera) — place it here before asking
  for that pipeline to run.
