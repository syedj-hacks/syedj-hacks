REPO CARDS — code-generated, not raster art

Each *-dark.svg / *-light.svg pair is a self-contained animated (SMIL fade+slide
on load) repo card. Regenerate by editing the SVG directly if a repo's
description, language, or stats change — there's no external asset to drop in.
